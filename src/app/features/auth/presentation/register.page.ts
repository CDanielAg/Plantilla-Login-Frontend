import { Component,inject,signal } from '@angular/core';import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms';import { RouterLink } from '@angular/router';import { AuthShell } from '../../../shared/ui/auth-shell';import { AuthApi } from '../data/auth.api';
@Component({imports:[AuthShell,ReactiveFormsModule,RouterLink],template:`<app-auth-shell title="Crea tu cuenta" subtitle="Empieza en menos de un minuto">@if(sent()){<div class="success" role="status"><strong>Revisa tu correo</strong><br>Enviamos un enlace a {{masked()}}.</div><p class="alternate"><a class="link" routerLink="/login">Volver al inicio de sesión</a></p>}@else{<form [formGroup]="form" (ngSubmit)="submit()">@if(error()){<p class="error" role="alert">{{error()}}</p>}<div class="field"><label for="name">Nombre</label><input id="name" autocomplete="name" formControlName="displayName"></div><div class="field"><label for="email">Correo electrónico</label><input id="email" type="email" autocomplete="email" formControlName="email"></div><div class="field"><label for="password">Contraseña</label><input id="password" type="password" autocomplete="new-password" formControlName="password"><div class="strength" aria-hidden="true"><span class="on"></span><span [class.on]="form.controls.password.value.length>=12"></span><span></span><span></span></div><small>Usa al menos 12 caracteres</small></div><label class="check"><input type="checkbox" formControlName="termsAccepted"><span>Acepto los términos y la política de privacidad</span></label><button class="button" [disabled]="form.invalid||busy()">{{busy()?'Creando cuenta…':'Crear cuenta'}}</button><p class="alternate">¿Ya tienes cuenta? <a class="link" routerLink="/login">Inicia sesión</a></p></form>}</app-auth-shell>`})
export class RegisterPage{private readonly fb=inject(FormBuilder);private readonly api=inject(AuthApi);readonly busy=signal(false);readonly error=signal('');readonly sent=signal(false);readonly masked=signal('');readonly form=this.fb.nonNullable.group({displayName:['',[Validators.required,Validators.maxLength(120)]],email:['',[Validators.required,Validators.email]],password:['',[Validators.required,Validators.minLength(12),Validators.maxLength(128)]],termsAccepted:[false,Validators.requiredTrue]});submit(){if(this.form.invalid)return;this.busy.set(true);this.api.register(this.form.getRawValue()).subscribe({next:r=>{this.masked.set(r.maskedEmail);this.sent.set(true);this.busy.set(false);},error:e=>{this.error.set(e.error?.detail||'No pudimos crear la cuenta.');this.busy.set(false);}});}}
