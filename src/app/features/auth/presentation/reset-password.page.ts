import { Component,inject,signal } from '@angular/core';import { ActivatedRoute,RouterLink } from '@angular/router';import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms';import { AuthShell } from '../../../shared/ui/auth-shell';import { AuthApi } from '../data/auth.api';
@Component({imports:[AuthShell,ReactiveFormsModule,RouterLink],template:`<app-auth-shell title="Crea una nueva contraseña" subtitle="Debe ser diferente a las anteriores">@if(done()){<div class="success"><strong>Tu contraseña fue actualizada</strong><br>Ya puedes iniciar sesión con tus nuevos datos.</div><p class="alternate"><a class="link" routerLink="/login">Iniciar sesión</a></p>}@else{<form [formGroup]="form" (ngSubmit)="submit()">@if(error()){<p class="error" role="alert">{{error()}}</p>}<div class="field"><label for="password">Nueva contraseña</label><input id="password" type="password" autocomplete="new-password" formControlName="password"><div class="strength"><span class="on"></span><span [class.on]="form.controls.password.value.length>=12"></span><span></span><span></span></div><small>Usa al menos 12 caracteres</small></div><div class="field"><label for="confirm">Confirmar contraseña</label><input id="confirm" type="password" autocomplete="new-password" formControlName="confirm"></div><button class="button" [disabled]="form.invalid||busy()">{{busy()?'Actualizando…':'Actualizar contraseña'}}</button><p class="alternate"><a class="link" routerLink="/login">Volver al inicio de sesión</a></p></form>}</app-auth-shell>`})
export class ResetPasswordPage{private readonly fb=inject(FormBuilder);private readonly route=inject(ActivatedRoute);private readonly api=inject(AuthApi);readonly busy=signal(false);readonly done=signal(false);readonly error=signal('');readonly token=this.route.snapshot.queryParamMap.get('token')||'';readonly form=this.fb.nonNullable.group({password:['',[Validators.required,Validators.minLength(12)]],confirm:['',Validators.required]});submit(){const v=this.form.getRawValue();if(this.form.invalid||v.password!==v.confirm){this.error.set('Las contraseñas deben coincidir.');return;}if(!this.token){this.error.set('Este enlace ya no es válido.');return;}this.busy.set(true);this.api.resetPassword(this.token,v.password).subscribe({next:()=>{this.done.set(true);this.busy.set(false);},error:e=>{this.error.set(e.error?.detail||'No pudimos actualizar la contraseña.');this.busy.set(false);}});}}
